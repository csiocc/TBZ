Beispiele für Authentisierung:
Login für Spiele/Cliente wie Steam, Blizzard, GoG usw
Login für Mails
Login für Homepages z.b Shopping
Login zu Windows
Handy entsperren mit FaceID/Fingerprint/Code

Beispiele für Authentifizierung
Rails projekte: Controller
configs
.env dateien für verschiedene projekte
Python fast API auth.py
Passieren für alle Logins die ich mache bei Spielen Mai, HPs, serverseitig und nicht auf meinem rechner.

Beispiele für Autorisierungen
Bei allen aufgezählten Logins nachdem man erfolgreich authentifiziert wurde.

Authentisierung:
Einloggen/Seine identität beweisen

Authentifizierung:
authentizität bestätigen nutzername/passwort check, allenfals 2faktor code / FaceID / Fingerprint prüfen

Autorisierung:
Zugriff nach erfolgreicher authentifizierung freigeben.